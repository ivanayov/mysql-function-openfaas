provider:
  name: faas
  gateway: http://127.0.0.1:8080

functions:
  dbinserter:
    lang: python3
    handler: ./dbinserter
    image: docwareiy/dbinserter
    environment_file:
      - env.yml
    secrets: 
    - secret-mysql-key
